name: "Manual: Execute - Send Warnings (CONFIRM Required)"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "CONFIRM" to send warnings to inactive channels'
        required: true
        type: string

jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      confirmed: ${{ steps.check.outputs.confirmed }}
    steps:
      - name: Validate confirmation
        id: check
        run: |
          if [ "${{ inputs.confirm }}" = "CONFIRM" ]; then
            echo "confirmed=true" >> $GITHUB_OUTPUT
            echo "Confirmation received - proceeding with warning execution"
          else
            echo "confirmed=false" >> $GITHUB_OUTPUT
            echo "ERROR: Must type CONFIRM to proceed"
            exit 1
          fi

  execute-warnings:
    needs: validate
    if: needs.validate.outputs.confirmed == 'true'
    uses: ./.github/workflows/slack-ploughshare.yml
    secrets: inherit
    with:
      command: 'send-warnings'
      dry_run: false
      additional_flags: ''
